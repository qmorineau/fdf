valid map (every row have same size, else stop program)

orthographic always square ? (need to adjust width and height to be square, or adapt it)
stereographic line drawing and point linking to other different than orthographic / isometric

z_scale automatic calculation ?

handle one press vs pressing with no release

set no repeat key => xset -r
set repeat key => xset r

protect win and mlx ptr


problem normal projection => z_scale and rotation x